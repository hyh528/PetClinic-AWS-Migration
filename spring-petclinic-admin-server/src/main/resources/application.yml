server:
  port: 9090
  servlet:
    context-path: /admin

spring:
  application:
    name: admin-server
  profiles:
    active: aws
  boot:
    admin:
      ui:
        enabled: true
      client:
        enabled: false  # Admin 서버는 클라이언트가 아님
      discovery:
        enabled: true
        services:
          - customers-service
          - vets-service
          - visits-service
  thymeleaf:
    check-template-location: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: when-authorized
