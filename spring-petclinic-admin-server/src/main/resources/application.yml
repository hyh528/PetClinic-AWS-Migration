spring:
  application:
    name: admin-server
  
  config:                                                
    import: [                                            
      "optional:aws-parameterstore:/petclinic/common/",  
      "optional:aws-parameterstore:/petclinic/dev/admin" 
    ]                                                    
  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mysql,aws}

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}

