server:
  port: 9090
  servlet:
    context-path: /admin

spring:
  application:
    name: admin-server
  profiles:
    active: aws
  boot:
    admin:
      ui:
        enabled: true
  thymeleaf:
    check-template-location: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: when-authorized
