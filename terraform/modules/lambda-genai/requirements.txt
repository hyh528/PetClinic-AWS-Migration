# Lambda GenAI 함수 의존성
# Python 3.11 런타임용

# AWS SDK (Lambda 런타임에 기본 포함되지만 명시적으로 지정)
boto3>=1.34.0
botocore>=1.34.0

# JSON 처리 및 유틸리티 (기본 라이브러리이지만 명시)
# json - 기본 라이브러리
# logging - 기본 라이브러리
# os - 기본 라이브러리
# traceback - 기본 라이브러리
# typing - 기본 라이브러리
# base64 - 기본 라이브러리