spring:
  application:
    name: customers-service

  config:
    import: [
      "optional:aws-parameterstore:/petclinic/common/", 
      "optional:aws-parameterstore:/petclinic/dev/customers",      
      "aws-secretsmanager:rds!cluster-0edf3242-4cb9-4b90-9896-52cc5068a5fb"
    ]
  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}
  
  
---
spring:
  config:
    activate:
      on-profile: mysql
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://petclinic-aurora-cluster-dev.cluster-chayuwmicqjy.ap-northeast-2.rds.amazonaws.com:3306/petclinic
    username: petclinic
    password: 1b*!RrOjx2~AIim2e>Cx(>0~F77P

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}
      paramstore:
        enabled: true
        prefix: /petclinic
        profile-separator: /
        fail-fast: false
      secretsmanager:
        enabled: true
        prefix: /petclinic
        fail-fast: false
