spring:
  application:
    name: vets-service
  boot:
    admin:
      client:
        url: http://petclinic-dev-alb-1211424104.us-west-2.elb.amazonaws.com/admin
        instance:
          service-base-url: http://petclinic-dev-alb-1211424104.us-west-2.elb.amazonaws.com/api/vets
          management-base-url: http://petclinic-dev-alb-1211424104.us-west-2.elb.amazonaws.com/api/vets
          health-url: http://petclinic-dev-alb-1211424104.us-west-2.elb.amazonaws.com/api/vets/actuator/health
  cloud:
    aws:
      region:
        static: ${AWS_REGION:us-west-2}
      paramstore:
        enabled: true
        prefix: /petclinic
        profile-separator: /
        fail-fast: false
      secretsmanager:
        enabled: true
        prefix: /petclinic
        fail-fast: false
  cache:
    cache-names: vets
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mysql,aws}

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: mysql
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    aws:
      region:
        static: ${AWS_REGION:us-west-2}
      paramstore:
        enabled: true
        prefix: /petclinic
        profile-separator: /
        fail-fast: false
      secretsmanager:
        enabled: true
        prefix: /petclinic
        fail-fast: false
